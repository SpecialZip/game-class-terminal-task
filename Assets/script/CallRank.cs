using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CallRank : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Rank.Instance.ShowRank();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
